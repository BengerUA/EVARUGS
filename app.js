$(document).ready(function(){
  $('.slider').slick({
    centerMode: true,
    slidesToShow: 1,
    autoplay: true,
    infinite: true,
    speed: 500,
    arrows: false,
    centerPadding: '60px',
  });
});

